part of '../bloc/sign_in_bloc.dart';

extension ForgotPasswordUC on SignInBloc {
  Future<void> _forgotPassword(ForgotPassword event, Emitter<SignInState> emit) async {
    //!Navigate to forgot password screen (GoRouter)
  }
}