# Copy to .env and fill in. .env is gitignored.

# Deepgram streaming STT API key (required for live recitation).
DEEPGRAM_API_KEY=

# --- Production only ---
# DJANGO_SECRET_KEY=
# DJANGO_ALLOWED_HOSTS=api.example.com,www.example.com
# CORS_ALLOWED_ORIGINS=https://app.example.com
